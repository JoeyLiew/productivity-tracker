import React from 'react';
import PropTypes from 'prop-types';
import './Home.css';

const Home = (props) => {
  return (
    <main className='home'>
      <p>Home</p>
    </main>
  );
};

Home.propTypes = {};

export default Home;
