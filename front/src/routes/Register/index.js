import React from 'react';
import PropTypes from 'prop-types';

const Register = (props) => {
  return (
    <div>
      <p>Register</p>
    </div>
  );
};

Register.propTypes = {};

export default Register;
